server:
  port: 8000
  error:
    include-stacktrace: never
    whitelabel:
      enabled: false


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_schedule
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
